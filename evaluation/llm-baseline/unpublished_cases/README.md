# Unpublished contamination-control cases

Place 6–9 fresh cases here before execution. Each case should contain:

- one clean baseline or a documented parent baseline;
- a schema-valid mutant;
- a neutral intended-behavior statement written before model execution;
- a private oracle;
- hashes and creation timestamp;
- no public repository exposure until all calls are complete.

Do not reuse the public mutant IDs. Suggested IDs: U001–U009. Keep the oracle under `private/` and expose only workflow files plus neutral intended behavior to the runner.
