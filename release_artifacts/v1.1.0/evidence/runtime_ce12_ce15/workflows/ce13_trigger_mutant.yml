name: CE13 trigger mutant
on:
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "pyproject.toml"
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - run: echo "CE13 mutant triggered"
