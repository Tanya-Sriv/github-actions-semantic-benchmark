schema_version: '0.1'
mutant_id: B004_TR02_001
baseline_id: B004
operator_id: TR02
oracle_strength: O3
fault_class: trigger_intent
expected_fault: The required tests/** path is removed from pull-request filters.
preconditions:
- id: P1
  description: The pull_request paths include tests/**.
  verification: static_check
- id: P2
  description: The intent manifest requires tests/**.
  verification: manual_review
mutation:
  target_file: .github/workflows/ci.yml
  target_path: on.pull_request.paths
  before:
  - src/**
  - tests/**
  - pyproject.toml
  after:
  - src/**
  - pyproject.toml
  single_fault_expected: !!bool 'true'
  mutant_sha256: f05f13e75b624f93608b1cca7468072d31eba12abedeb5df25ba4fed479f5035
ground_truth:
  basis:
  - type: intent_manifest
    reference: B004 required_paths.pull_request
  violated_property: All intent-required change classes must remain covered by event path filters.
  expected_behavior: Pull requests that modify only tests can bypass validation.
  execution_confirmation_required: !!bool 'false'
  execution_confirmation_status: not_required
detection_criteria:
  counts_as_detection:
  - Diagnostic identifies tests/** as missing from required pull-request path coverage.
  - Diagnostic reports that test-only changes no longer trigger validation.
  does_not_count_as_detection:
  - A generic glob syntax warning.
  - Any warning unrelated to required path coverage.
  acceptable_equivalent_diagnostics:
  - Required pull_request path tests/** is absent.
expected_tool_behavior:
  actionlint:
    expectation: likely_miss
    basis: reasoned_expectation
    notes: No documented rule specifically covers this declared or cross-element operational relation.
  zizmor:
    expectation: likely_miss
    basis: reasoned_expectation
    notes: Security-focused analysis is not expected to identify this non-security operational relation.
  poutine:
    expectation: likely_miss
    basis: reasoned_expectation
    notes: Built-in security rules are not expected to identify this non-security operational relation.
  semantic_checker:
    expectation: detect
    basis: reasoned_expectation
    notes: Compare trigger filters against the declared path set.
manual_review:
  required: !!bool 'true'
  review_status: not_started
  review_notes: Regenerated after baseline and oracle revisions. Manual semantic re-review required.
limitations:
- Path-filter semantics can be subtle when include/exclude lists are combined; this pilot uses inclusion-only filters.
