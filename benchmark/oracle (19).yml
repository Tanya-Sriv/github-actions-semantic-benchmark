schema_version: '0.1'
mutant_id: B002_MX03_001
baseline_id: B002
operator_id: MX03
oracle_strength: O3
fault_class: coverage_intent
expected_fault: fail-fast is enabled even though the manifest requires all matrix combinations to complete.
preconditions:
- id: P1
  description: 'The strategy explicitly sets fail-fast: false.'
  verification: static_check
- id: P2
  description: 'The intent manifest sets fail_fast_allowed: false.'
  verification: manual_review
mutation:
  target_file: .github/workflows/ci.yml
  target_path: jobs.test.strategy.fail-fast
  before: !!bool 'false'
  after: !!bool 'true'
  single_fault_expected: !!bool 'true'
  mutant_sha256: c63bc964373c9dcfd8e60a7ce9ddbb3b8acc021dc504b51ed32a35781449351f
ground_truth:
  basis:
  - type: intent_manifest
    reference: B002 required_matrix entries require complete execution
  - type: github_specification
    reference: GitHub matrix fail-fast semantics
  violated_property: When complete evidence is required, one matrix failure must not cancel other in-progress or queued combinations.
  expected_behavior: A failing matrix cell can cancel remaining combinations, producing incomplete coverage evidence.
  execution_confirmation_required: !!bool 'true'
  execution_confirmation_status: pending
detection_criteria:
  counts_as_detection:
  - Diagnostic identifies that fail-fast violates the declared complete-matrix policy.
  - Diagnostic reports that required matrix combinations may be cancelled.
  does_not_count_as_detection:
  - A generic mention that fail-fast is enabled.
  - Any warning unrelated to required completion.
  acceptable_equivalent_diagnostics:
  - fail-fast must remain false because all combinations are required.
expected_tool_behavior:
  actionlint:
    expectation: likely_miss
    basis: reasoned_expectation
    notes: No documented rule specifically covers this declared or cross-element operational relation.
  zizmor:
    expectation: likely_miss
    basis: reasoned_expectation
    notes: Security-focused analysis is not expected to identify this non-security operational relation.
  poutine:
    expectation: likely_miss
    basis: reasoned_expectation
    notes: Built-in security rules are not expected to identify this non-security operational relation.
  semantic_checker:
    expectation: detect
    basis: reasoned_expectation
    notes: Combine strategy semantics with intent-declared complete execution.
manual_review:
  required: !!bool 'true'
  review_status: not_started
  review_notes: Regenerated after baseline and oracle revisions. Manual semantic re-review required.
limitations:
- Execution confirmation requires a controlled failing matrix cell.
- Cancellation timing may vary across runs.
